---
import Layout from "../layouts/Layout.astro";
import CircularRotatingText from "../components/CircularRotatingText.svelte";
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <title>Circular Text Demo - GSAP MotionPathPlugin</title>
        <style>
            body {
                margin: 0;
                padding: 0;
                font-family:
                    -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto,
                    sans-serif;
                background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
                min-height: 100vh;
            }
            .demo-container {
                padding: 2rem;
                max-width: 1200px;
                margin: 0 auto;
            }
            .demo-section {
                margin-bottom: 4rem;
                text-align: center;
            }
            .demo-title {
                font-size: 2rem;
                font-weight: 700;
                color: #1f2937;
                margin-bottom: 3rem;
                text-align: center;
            }
            .section-title {
                font-size: 1.5rem;
                font-weight: 600;
                color: #374151;
                margin-bottom: 1.5rem;
            }
            .section-description {
                color: #6b7280;
                margin-bottom: 2rem;
                max-width: 500px;
                margin-left: auto;
                margin-right: auto;
                line-height: 1.6;
            }
            .demo-grid {
                display: grid;
                grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                gap: 3rem;
                margin-top: 3rem;
            }
            .demo-item {
                display: flex;
                flex-direction: column;
                align-items: center;
                padding: 2rem;
                background: rgba(255, 255, 255, 0.7);
                border-radius: 12px;
                box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
                backdrop-filter: blur(10px);
            }
            .demo-item h3 {
                font-size: 1.25rem;
                font-weight: 600;
                color: #374151;
                margin-bottom: 1rem;
            }
            .demo-item p {
                color: #6b7280;
                font-size: 0.875rem;
                margin-bottom: 1.5rem;
                text-align: center;
            }
            .controls-info {
                background: #fef3c7;
                border: 1px solid #f59e0b;
                border-radius: 8px;
                padding: 1rem;
                margin: 2rem auto;
                max-width: 600px;
                color: #92400e;
                font-size: 0.875rem;
            }
        </style>
    </head>
    <body>
        <div class="demo-container">
            <h1 class="demo-title">
                Circular Rotating Text with GSAP MotionPathPlugin
            </h1>

            <div class="controls-info">
                <strong>🎯 Plugin Used:</strong> GSAP MotionPathPlugin - Characters
                follow a precise circular SVG path for smooth animation
            </div>

            <!-- Main Demo -->
            <div class="demo-section">
                <h2 class="section-title">"Si mangia e si beve" - Original</h2>
                <p class="section-description">
                    Default circular text animation using GSAP's
                    MotionPathPlugin. Each character follows the same circular
                    path with perfect spacing.
                </p>
                <CircularRotatingText client:load />
            </div>

            <!-- Multiple Variations -->
            <div class="demo-grid">
                <div class="demo-item">
                    <h3>Large & Colorful</h3>
                    <p>Bigger radius with custom colors and slower rotation</p>
                    <CircularRotatingText
                        client:load
                        radius={180}
                        textColor="#dc2626"
                        circleColor="#fef2f2"
                        fontSize="1.75rem"
                        duration={12}
                    />
                </div>

                <div class="demo-item">
                    <h3>Counter-clockwise</h3>
                    <p>Reverse rotation direction with green theme</p>
                    <CircularRotatingText
                        client:load
                        radius={140}
                        rotationDirection={-1}
                        textColor="#059669"
                        circleColor="#ecfdf5"
                        fontSize="1.25rem"
                        duration={8}
                    />
                </div>

                <div class="demo-item">
                    <h3>Fast & Purple</h3>
                    <p>Quick rotation with purple styling</p>
                    <CircularRotatingText
                        client:load
                        radius={120}
                        textColor="#7c3aed"
                        circleColor="#f3e8ff"
                        fontSize="1rem"
                        duration={4}
                    />
                </div>

                <div class="demo-item">
                    <h3>Elegant & Minimal</h3>
                    <p>Clean design with subtle colors</p>
                    <CircularRotatingText
                        client:load
                        radius={160}
                        textColor="#374151"
                        circleColor="#f9fafb"
                        fontSize="1.5rem"
                        duration={15}
                    />
                </div>

                <div class="demo-item">
                    <h3>Bold & Orange</h3>
                    <p>High contrast with bold styling</p>
                    <CircularRotatingText
                        client:load
                        radius={130}
                        textColor="#ea580c"
                        circleColor="#fff7ed"
                        fontSize="1.4rem"
                        duration={6}
                    />
                </div>

                <div class="demo-item">
                    <h3>Ocean Theme</h3>
                    <p>Blue tones with medium speed</p>
                    <CircularRotatingText
                        client:load
                        radius={150}
                        textColor="#0284c7"
                        circleColor="#f0f9ff"
                        fontSize="1.3rem"
                        duration={10}
                    />
                </div>
            </div>

            <div
                style="margin-top: 4rem; padding: 2rem; background: rgba(255, 255, 255, 0.8); border-radius: 12px; text-align: center;"
            >
                <h3 style="color: #374151; margin-bottom: 1rem;">
                    Technical Details
                </h3>
                <ul
                    style="text-align: left; max-width: 600px; margin: 0 auto; color: #6b7280; line-height: 1.6;"
                >
                    <li>
                        <strong>GSAP MotionPathPlugin:</strong> Uses SVG path data
                        for precise circular motion
                    </li>
                    <li>
                        <strong>Svelte 5:</strong> Modern reactive framework with
                        excellent performance
                    </li>
                    <li>
                        <strong>Tailwind CSS:</strong> Utility-first styling for
                        consistent design
                    </li>
                    <li>
                        <strong>Smooth Animation:</strong> Hardware-accelerated transforms
                        with optimal performance
                    </li>
                    <li>
                        <strong>Customizable:</strong> Radius, colors, speed, and
                        direction all configurable
                    </li>
                </ul>
            </div>
        </div>
    </body>
</html>
