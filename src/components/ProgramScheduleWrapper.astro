---
import ProgramSchedule from "../components/ProgramSchedule.svelte";
import { getProgramData } from "../lib/notion";

// const env = Astro.locals.runtime || { env: {} };

const programSections = await getProgramData();
---

<section
    class="relative flex flex-col justify-center items-start w-full h-auto px-4 sm:px-6 lg:px-40 pt-8 sm:pt-12 lg:pt-16"
>
    <ProgramSchedule client:load {programSections} />
</section>
